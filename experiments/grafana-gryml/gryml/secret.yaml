apiVersion: v1
kind: Secret
metadata:
  name: "" #{chart.name}
  labels: {} #[merge]{chart.labels}
  namespace: "" #{grafana.namespace}

type: Opaque
data:

  #{admin.adminUser|b64enc}
  admin-user: admin

  #[if admin.adminPassword]{admin.adminPassword|b64enc}[else]{40|randstr|b64enc}
  admin-password: password

  #[if ldap.existingSecret]{ldap.config|b64enc}
  ldap-toml: ""

