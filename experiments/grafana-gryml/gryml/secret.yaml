apiVersion: v1
kind: Secret
metadata:
  name: "" #{chart.name}
  labels: {} #[merge]{chart.labels}
  namespace: "" #{grafana.namespace}

type: Opaque
data:

  #{adminUser|b64enc}
  admin-user: admin

  #[if adminPassword]{adminPassword|b64enc}[else]{randAlphaNum(40)|b64enc}
  admin-password: password

  #[if ldap.existingSecret]{ldap.config|b64enc}
  ldap-toml: ""

